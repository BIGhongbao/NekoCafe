# ZAP baseline rules for NekoCafe PoC APIs.
# 当前实验三 PoC 只暴露本地 HTTP 服务，未配置正式 TLS、鉴权和网关。
# 课程提交时保留中高危问题并在 D4-5/D4-6 中说明处置计划，不把真实风险静默白名单化。

# 示例：如果 ZAP 对本地测试环境的缺失 HSTS 报告为低风险，可记录为 WARN。
10035	WARN	Strict-Transport-Security Header Not Set - local PoC HTTP endpoint
