[pytest]
testpaths = unit property integration contract
pythonpath = .
addopts = -q --strict-markers --junitxml=reports/junit.xml
markers =
    unit: fast service-level tests
    property: Hypothesis property based tests
    integration: cross-service or deployment smoke tests
    contract: provider/consumer contract checks

